# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# --- Database (PostgreSQL + pgvector) ---
sqlalchemy>=2.0.30
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
pgvector>=0.2.5
alembic>=1.13.1

# --- LLMs (Ollama - Qwen / Llama) ---
ollama>=0.2.1

# --- Embeddings (BGE / Nomic Embed) ---
sentence-transformers>=3.0.1
torch>=2.3.0
numpy>=1.26.4

# --- File parsing ---
PyMuPDF>=1.24.5
python-docx>=1.1.2
openpyxl>=3.1.2

# --- Automation (filesystem watching) ---
watchdog>=4.0.1

# --- Search ---
# ripgrep is a system binary, not a Python package.
# Install separately: https://github.com/BurntSushi/ripgrep#installation
# pgvector search is covered by the pgvector package above.

# --- Utilities ---
httpx>=0.27.0
tenacity>=8.3.0
